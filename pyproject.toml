[project]
name = "silabs-firmware-builder"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "ruamel.yaml",
    "universal-silabs-flasher>=0.0.25",
    "pyelftools",
]

[tool.uv]
package = false
