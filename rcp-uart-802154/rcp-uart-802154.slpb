---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "rcp-uart-802154"
steps:
- task: "usage"
  input: "{{build_dir}}/{{project_name}}.out"
  map: "{{build_dir}}/{{project_name}}.map"
